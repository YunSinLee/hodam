# 8쪽 그림책 베타 운영 기준

정식 출시 전 기준은 새로 만드는 8쪽 그림책이다. 공개 검색 페이지는 계속 운영하고, 첫 이용자가 그림책을 완성하고 다시 읽는 경험을 우선 검증한다.

## 책장과 기존 기록

- `/my-story`는 새 그림책을 기본으로 보여준다. 첫 4쪽은 결말을 고를 수 있고, 결말까지 저장하면 8쪽으로 표시한다. 본문 완성과 삽화 준비는 별도이므로 목록의 상태는 `8쪽 · 결말 완성`으로 표기한다.
- `/my-story/archive`는 기존 동화와 내용 확인이 필요한 기록을 보관한다. 원문, 영어, 그림, 소유권을 바꾸거나 8쪽으로 자동 변환하지 않는다.
- 분류는 저장된 원고가 그림책 형식으로 해석되는지를 기준으로 한다. 생성일이나 `openai_thread_id` 접두어로 나누지 않는다. 보관함에서는 읽을 수 있는 동화를 먼저 보여주고, 글이 없는 기록 등은 `내용 확인이 필요한 기록` 필터로 확인한다.
- 예전 기록이 있는 계정에는 기본 책장에서 보관함 링크와 기록 수를 표시한다. 예전 동화를 읽고 돌아오면 보관함으로 이동한다.
- 보관함도 개인 페이지다. 인증·소유권 보호와 검색 제외를 유지한다.
- 2026-09-14 확인 시 예전 형식 기록 706건이 9개 계정에 걸쳐 있었고 테스트 계정 표시가 없었다. 테스트용이라고 단정할 수 없어 삭제하지 않았다. 이 수치는 `openai_thread_id` 접두어를 기준으로 한 보존 대상 조사이며, 완성된 동화 수가 아니다.

## 출시 전 확인할 흐름

1. 로그인 전 작성한 입력이 로그인 후에도 유지된다.
2. 첫 계정의 프로필·곶감·빈 책장이 준비된다.
3. 첫 4쪽이 검수를 통과한 뒤 저장되고 곶감은 한 번만 사용된다.
4. 첫 4쪽을 읽고 세 가지 행동 중 하나를 고르면 결말 4쪽으로 이어진다.
5. 8쪽 글과 삽화 8장을 확인한다. 누락된 삽화는 기존 글과 이미지를 보존하며 다시 요청할 수 있다.
6. 책장을 떠났다가 돌아와도 내용·선택한 결말·그림이 유지된다.
7. 예전 동화는 보관함에서 원문 그대로 읽고, 새 그림책과 혼동하지 않는다.

원고 검수에 실패한 글은 저장하지 않는다. 실패율과 재시도 성공률은 베타 점검 항목이다. 한 번의 성공을 모든 입력에 대한 품질 보증으로 취급하지 않는다.

새 실패 로그 `picturebook_generation_failed`는 시작·결말 단계와 모델 연결·시간 초과·응답 형식 등 고정된 분류만 남긴다. 원고·입력·공급자 오류 본문은 기록하지 않는다. 이미 처리된 `GenerationError`는 이 로그를 남기지 않으므로, 이 로그 건수만으로 전체 품질 거절률이나 사용자 재시도 성공률을 계산하지 않는다.

## 테스트 데이터

새 운영 QA 계정에는 `hodam_test_user: true`와 용도를 표시하고 가상의 아이 정보를 사용한다. 테스트용 원고·빈 기록도 해당 계정에만 만든다. 실제 사용자 기록은 QA 목적으로 변환하거나 삭제하지 않는다.

OAuth 제공자의 신규 가입, 앱의 첫 계정 초기화, 원고 생성, 결말 생성, 삽화 생성, 재방문 검증은 각각 기록한다. 관리자 API로 준비한 테스트 계정의 로그인 검증을 Google·카카오 신규 가입 완료로 기록하지 않는다.

## 2026-09-14 확인 결과

- 새로 표시한 QA 계정과 가상의 도서관 경험으로 로컬 프로덕션 빌드에서 실제 Supabase·OpenAI 흐름을 확인했다. 로그인 전 입력 복원, 첫 계정 초기 곶감 10개와 빈 책장, 첫 4쪽 생성, 행동 선택, 결말 4쪽과 삽화 8장, 책장 재열기가 동작했다. 완성 후 잔액은 9개로 유지됐다.
- 같은 QA 계정에만 예전 동화 1건과 빈 기록 1건을 추가해 기본 책장과 보관함 분리, 원문·영어 읽기, 보관함 복귀를 확인했다. 기존 계정의 기록은 수정하지 않았다.
- 390×844 브라우저 화면에서도 기본 책장·보관함·빈 기록 필터·상세 안내·보관함 복귀를 확인했다. 가로 스크롤 없이 문서 너비가 390px이었고 브라우저 오류는 없었다. 실제 모바일 기기를 사용한 검증은 아니다.
- 개선 전 같은 입력의 UI 생성 시도 2회가 실패했고 기록과 곶감 차감은 없었다. 첫 실패는 선택지 인과 검수에서 거절됐고 두 번째는 당시 로그만으로 원인을 확정하지 못했다. 별도 모델 평가에서 무관한 선택지와 보호자 배경의 과도한 거절을 확인해 지침을 보완했다. 개선 후 UI 생성 1회가 결말·삽화까지 완료됐다.
- 자동 테스트 189개 파일·1,258개, 프로덕션 빌드, ESLint·타입 검사, 실제 모델 검수 사례 9개가 통과했다. 허용할 보호자 동석과 거절할 선행 행동을 각각 평가에 포함한다.
- Google·카카오 제공자 자체의 신규 가입과 모바일 실기기 검증은 이번 QA에 포함되지 않았다. 아래 추가 합성 표본의 성공도 모든 입력의 품질이나 전체 생성 실패율이 해결됐다는 보장은 아니다.

[그림책 품질 운영 가이드](picturebook-quality.md)의 편집 배포 조건에 따라 변경한 생성기로 세 연령 원고를 새로 만들고 24쪽 전체를 읽었다. 세 편 모두 작성·독립 검수만으로 통과했으며 교정 호출은 없었다. 원래 사건, 선택한 행동의 실제 수행, 잠자리로 이어지는 흐름과 문장의 의미를 확인했다.

| 합성 사례 | 선택과 실제 결말 | 완결 | 모델 호출 | 텍스트 관측 시간 |
| --- | --- | --- | --- | --- |
| 6세 / 어두운 밤 / A | 인형을 가슴에 안고 촉감과 호흡이 가라앉으며 같은 방에서 마무리 | 8쪽 | 4회 | 약 36초 |
| 3세 / 삽 나누기 / B | 삽을 가운데 놓고 친구가 사용한 뒤 돌려받으며 잠자리로 연결 | 8쪽 | 4회 | 약 31초 |
| 10세 / 모둠 활동 / C | 자신이 그릴 부분을 실제 대사로 말하고 모둠의 반응과 잠자리로 연결 | 8쪽 | 4회 | 약 36초 |

실행 명령은 `node scripts/qa-picturebook.mjs --run-live --label beta-three-ages`다. 이 추가 평가는 실제 모델로 텍스트만 생성하며 DB 쓰기·삽화 생성·곶감 차감은 하지 않는다. 앞의 새 계정 전체 흐름 QA와 별개다. 작성·검수 모델은 모두 `gpt-5.4-2026-03-05`였고 총 관측 사용량은 52,114토큰이다. 시간은 인증·저장·그림까지 포함한 서비스 완료 시간이 아니다.

재현 자료는 Git에서 제외된 `reports/local/picturebook-evals/beta-three-ages/`의 `manifest.json`, `report.md`, 사례별 JSON에 보관했다. 생성기 `src/app/api/langchain.ts`의 SHA-256은 `b37207108ee182e34e05e47a2e16eeae36d23b831743c0138582897b70fa0c2d`이며 `ad6586b`의 소스와 일치한다. 개선 전 실패 기록은 유지했다.
